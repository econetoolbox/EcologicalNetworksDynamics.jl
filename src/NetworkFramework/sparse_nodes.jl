# Typical refinment for a component bringing a new field
# to a network *sub*class, with a `sparse` logic.
# This is abstracted over by `define_node_field_component()`,
# but gather all the *sub*class-specific code here,
# unless this would make it much more difficult to maintain.
